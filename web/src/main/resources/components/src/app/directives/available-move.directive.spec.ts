import { AvailableMoveDirective } from './available-move.directive';

describe('AvailableMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new AvailableMoveDirective();
    expect(directive).toBeTruthy();
  });
});
